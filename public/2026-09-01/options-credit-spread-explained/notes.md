# Notes — Options Credit Spread Explained

## User Context
- User wants a coherent learning resource, not raw notes.
- User asked that future research on options credit spreads be added into the same playbook.

## Key Decisions
- Use `inputs/reference/options-credit-spreads-playbook.md` as the canonical long-lived playbook location.
- Mirror the current version into session outputs for retrieval by date/session.
- Keep the structure educational: concepts first, then formulas, examples, and risk controls.

## Observations
- `web_search`/`web_extract` were unavailable due to Firecrawl not being installed, so research was gathered via direct HTTP requests and browser inspection.
- Fidelity pages were blocked with HTTP 403, so OIC/OCC and Options Playbook were used instead.
- A live SPY options-chain example was captured from Yahoo Finance and included as a worked example.
