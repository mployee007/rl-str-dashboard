# Notes — BnB Kanban Board

## User Context
- User wants a brand new separate Kanban board for the AirBnB management/consultant business.
- App stack must be Next.js + Tailwind CSS.
- Repo name must be `BnB`.
- New items added in the board should be committed and pushed to GitHub automatically.

## Key Decisions
- Use a separate local repo directory rather than modifying the current STR dashboard repo.

## Observations
- `gh` CLI is not installed; HTTPS git credentials exist for GitHub user `mployee007`.
